class Function {
    int addr;

    int funcTest = (int addr) => {
        addr = 1;

        // anonymous function
        addr = (int addr) => {
            return addr * 2;
        }(addr);

        // closure
        addr = (void) => {
            return addr + 5;
        }();

        return addr;
    };
}
