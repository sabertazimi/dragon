/*
 * @class: computer
 * @brief: computer class
 */
class Computer {
    // cpu for pc
    int cpu;

    void crash = (int numTimes) => {
        int i;
        int hexical;

        hexical = 0x15;
        hexical = 0X05;

        for (i = 0; i < numTimes; i = i + 1) {
            Print("sad\n");
        }
    }
}

/*
 * @class: mac
 * @brief: mac os pc
 */
class Mac extends Computer {
    // mouse for mac
    int mouse;

    /*
     * @override
     */
    void crash = (int numTimes) => {
        Print("ack!");
    }

    int arrayTest = (int[] arr) => {
        int i;

        for (i = 0; i < arr.length(); i = i + 1) {
            if (arr.getElem(i) < 0) {
                arr.setElem(i, -arr.getElem(i));
            } else {
                arr[i] = arr[i] + i;
            }
        }
    }
}

/*
 * @class: main
 * @brief: program entry
 */
class Main {
    int main = (void) => {
        class Mac powerbook;
        powerbook = new Mac();
        powerbook.Crash(2);
    }
}
