class Computer {
    // cpu for pc
    int cpu;

    void crash = (int numTimes) => {
        Print(numTimes);
        return void;
    };

    void superMethod = (void) => {
        return;
    };
}

class Mac extends Computer {
    // mouse for mac
    int mouse = cpu;

    /*
     * @override
     */
    int crash = (int numTimes) => {
        // non-exist field both in this and super
        this.Cpu = numTimes;

        // inherits from super
        this.cpu = numTimes;

        Print("ack!");

        return numTimes + 1;
    };

}

class OSX extends Mac {
    int superSuperField = (void) => {
        // from class Computer
        return cpu;
    };
}

class Error extends Main {
    int main = (void) => {
        return 0;
    };
}

class Main {
    int main = (void) => {
        // undefined class
        class UnkownType unkown;

        // invalid access to non-exist field
        int a = this.field;

        class Mac powerbook= new Mac();

        // invalid access to private field
        int b = powerbook.mouse;

        // non-exist function
        powerbook.Crash(2);
        // incomptible arguments
        powerbook.crash();

        // method in super class
        powerbook.superMethod();

        return 0;
    };
}
