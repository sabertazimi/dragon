class Computer {
    // cpu for pc
    int cpu;

    void crash = (int numTimes) => {
        Print(numTimes);
        return void;
    };
}

class Mac extends Computer {
    // mouse for mac
    int mouse;

    /*
     * @override
     */
    int crash = (int numTimes) => {
        this.Cpu = numTimes;
        this.cpu = numTimes;
        Print("ack!");
        return numTimes + 1;
    };

}

class Error extends Main {
    int main = (void) => {
        return 0;
    };
}

class Main {
    int main = (void) => {
        this.field;
        class Mac powerbook;
        powerbook = new Mac();
        powerbook.Crash(2);
        return 0;
    };
}
