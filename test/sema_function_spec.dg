class Function {
    int addr = 1;

    int funcTest = (int addr) => {
        addr = 1;
        addrr = 5;

        // incompatiable between formals and actuals
        int sayHelloVar = int (void) => {
            Print("Hello World!\n");
            return 0;
        }(3);

        return addr;
    };

    void missingReturn = (void) => {
        // no return statement;
    };

    bool errorRetType = (int num) => {
        return num;
    };
}

class Main {
    int main = (void) => {
        class Function func = new Function();
        int a = func.nonExistFunction;
        return a;
    };
}
