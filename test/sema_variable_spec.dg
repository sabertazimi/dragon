class Variable {
    int[][] arr;

    int varTest = (void) => {
        // redefined variable
        int saber;
        bool saber;

        // error initialization(wrong dimension)
        arr = new int[10];

        return saber;
    };
}

class Main {
    void main = (void) => {
        return;
    };
}
